mixin price-result(properties = {})

  -
    const {
      title = "Итого",
      between = "",
      price = "0₽",
    } = properties

  section.price-result
    .price-result__title #{title}
    .price-result__between #{between}
    .price-result__price #{price}